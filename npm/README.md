# @webboxescom/agentman (npm)

The npm distribution downloads the matching GitHub release binary for macOS ARM64, Windows x64, Linux x64, or Linux ARM64 during `npm i -g @webboxescom/agentman`. The global command is `agentman`.

```bash
npm i -g @webboxescom/agentman
agentman
```
