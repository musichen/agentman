# agentman (npm)

The npm distribution downloads the matching signed GitHub release binary for macOS ARM64, Windows x64, Linux x64, or Linux ARM64 during `npm i -g agentman`.

```bash
npm i -g agentman
agentman
```
